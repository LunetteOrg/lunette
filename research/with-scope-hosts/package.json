{
  "name": "@lntt/research-with-scope-hosts",
  "version": "0.0.0",
  "private": true,
  "description": "Research prototype, NOT a product: the same routes as research/no-scope-hosts, this time composed with @lntt/scope's step primitive and NO carrier — args typed by an explicit generic. Measures what composition alone buys once vocabulary is off the table.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@lntt/scope": "workspace:*",
    "@lntt/wire": "workspace:*",
    "express": "5.2.1",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/express-serve-static-core": "^5.1.1",
    "@types/node": "^24.13.2",
    "@types/supertest": "^7.2.1",
    "supertest": "^7.2.2",
    "typescript": "^5.9.2",
    "vitest": "^4.1.0"
  }
}
