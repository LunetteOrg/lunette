{
  "name": "@lntt/research-outcome-vocabulary",
  "version": "0.0.0",
  "private": true,
  "description": "Research prototype, NOT a product: each carrier extension owns its own vocabulary (in and out), the intent is inferred from what a guard/leaf returns, and it is checked twice — at the scope definition and at the mount.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "typescript": "^5.9.2",
    "vitest": "^4.1.0"
  }
}
