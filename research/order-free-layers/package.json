{
  "name": "@lntt/research-order-free-layers",
  "version": "0.0.0",
  "private": true,
  "description": "Research prototype, NOT a product: order-free layer resolution via runtime `requires` keys + topological sort. Prior art for parallel boot (TODO.md story 3).",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.0"
  }
}
