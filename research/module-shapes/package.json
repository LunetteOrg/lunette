{
  "name": "@lntt/research-module-shapes",
  "version": "0.0.0",
  "private": true,
  "description": "Research exploration, NOT a product: authoring shapes for feature modules over @lntt/wire — code-oriented vs fluent chain steps, per-leaf fragments, the window as a named step, and a point-free leaves() helper.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@lntt/wire": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.0"
  }
}
