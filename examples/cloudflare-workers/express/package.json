{
  "name": "@lntt/example-workers-express",
  "version": "0.0.0",
  "private": true,
  "description": "Example: the Express pack, unchanged, serving from a Cloudflare Worker through cloudflare:node's httpServerHandler.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "wrangler types && tsc --noEmit"
  },
  "dependencies": {
    "@lntt/integration": "workspace:*",
    "@lntt/scope": "workspace:*",
    "@lntt/wire": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.0",
    "@types/express": "^5.0.6",
    "@types/node": "^24.13.2",
    "express": "^5.2.1",
    "typescript": "^5.9.2",
    "vitest": "^4.1.0",
    "wrangler": "^4.126.0",
    "zod": "^3.25.76"
  }
}
