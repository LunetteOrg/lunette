{
  "name": "@lntt/example-workers-bare",
  "version": "0.0.0",
  "private": true,
  "description": "Example: the scope runtime on Cloudflare Workers with NO pack \u2014 a fetch handler calling runScope by hand, where the lazy build is mandatory rather than advisable.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "wrangler types && tsc --noEmit"
  },
  "dependencies": {
    "@lntt/integration": "workspace:*",
    "@lntt/scope": "workspace:*",
    "@lntt/wire": "workspace:*",
    "@standard-schema/spec": "^1.0.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.0",
    "typescript": "^5.9.2",
    "vitest": "^4.1.0",
    "wrangler": "^4.126.0"
  }
}
