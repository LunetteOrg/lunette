{
  "name": "@lntt/example-hono",
  "version": "0.0.0",
  "private": true,
  "description": "Example: the shared app, mounted on Hono with @lntt/scope/hono — the route gate reading the schema, a base scope branched per route, and the typed RPC client the transparent mount exists for.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@lntt/example-app": "workspace:*",
    "@lntt/scope": "workspace:*",
    "hono": "4.13.5",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "typescript": "catalog:",
    "vitest": "^4.1.0"
  }
}
