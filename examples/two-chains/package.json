{
  "name": "@lntt/example-two-chains",
  "version": "0.0.0",
  "private": true,
  "description": "Example: two independent chains — two products in one process — mounted on the same Express app, each serving its own routes with its own lifecycle.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@lntt/integration": "workspace:*",
    "@lntt/scope": "workspace:*",
    "@lntt/wire": "workspace:*"
  },
  "devDependencies": {
    "@standard-schema/spec": "^1.0.0",
    "@types/express": "^5.0.6",
    "@types/node": "^24.0.0",
    "express": "^5.2.1",
    "typescript": "^5.9.2",
    "vitest": "^3.2.0",
    "zod": "^3.24.1"
  }
}
