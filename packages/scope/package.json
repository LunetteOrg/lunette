{
  "name": "@lntt/scope",
  "version": "0.0.0",
  "description": "The host-agnostic scope runtime for @lntt/wire. ONE primitive — a step wrapping the rest of the fold — and a scope IS the function that runs it. Framework-free: carriers and extensions ship as tree-shakable subpaths and the core names none. Being rebuilt on the settled step formula (#30); the carriers, the extensions and the host mounts land on it next.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./express": "./src/express/index.ts",
    "./hono": "./src/hono/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^24.13.2",
    "@types/supertest": "^7.2.1",
    "express": "5.2.1",
    "hono": "4.13.5",
    "supertest": "^7.2.2",
    "typescript": "^5.9.2",
    "vitest": "^4.1.0"
  },
  "peerDependencies": {
    "express": ">=5",
    "hono": ">=4"
  },
  "peerDependenciesMeta": {
    "express": {
      "optional": true
    },
    "hono": {
      "optional": true
    }
  }
}
