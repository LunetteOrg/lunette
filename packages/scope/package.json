{
  "name": "@lntt/scope",
  "version": "0.0.0",
  "description": "The host-agnostic scope runtime for @lntt/wire: a scope (guard/leaf) model with a Standard-Schema input contract, run as a typed fold. Framework-free — the host integrations live in @lntt/integration; carrier extensions ship as tree-shakable subpaths (./request).",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./request": "./src/extensions/request.ts",
    "./body": "./src/extensions/body.ts",
    "./cookies": "./src/extensions/cookies.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@standard-schema/spec": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.0",
    "zod": "^3.25.0"
  }
}
